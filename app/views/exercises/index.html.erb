<div class="container-small">
  <%= link_to "Revenir à l'accueil et générer un tabata", '/', {class: "btn btn-primary"} %>
</div>

<div class="text-center">
  <h1> Les instruments de torture:</h1>
</div>

<div class="row text-center">
    <h2>Cardio</h2>
</div>
<div class="row">
  <% Exercise.where('exercise_type = ?', 'Cardio').each do |exercise|  %>
    <div class="col-xs-12 col-sm-6 col-md-3">
      <%= render 'exercise_card', exercise: exercise %>
    </div>
  <% end %>
</div>

<div class="container-small">
  <div class="row">
    <div class="text-center">
      <h2>Abdos</h2>
    </div>
    <% Exercise.where('exercise_type = ?', 'Abdos').each do |exercise|  %>
    <div class="col-xs-12 col-sm-6 col-md-3">
      <%= render 'exercise_card', exercise: exercise %>
    </div>
  <% end %>
  </div>
</div>

<div class="container-small">
  <div class="row">
    <div class="text-center">
      <h2>Pecs</h2>
    </div>
    <% Exercise.where('exercise_type = ?', 'Pecs').each do |exercise|  %>
    <div class="col-xs-12 col-sm-6 col-md-3">
      <%= render 'exercise_card', exercise: exercise %>
    </div>
  <% end %>
  </div>
</div>

<div class="container-small">
  <div class="row">
    <div class="text-center">
      <h2>Dos</h2>
    </div>
    <% Exercise.where('exercise_type = ?', 'Dos').each do |exercise|  %>
    <div class="col-xs-12 col-sm-6 col-md-3">
      <%= render 'exercise_card', exercise: exercise %>
    </div>
  <% end %>
  </div>
</div>

<div class="container-small">
  <div class="row">
    <div class="text-center">
      <h2>Epaules / Tris</h2>
    </div>
      <% [Exercise.where('exercise_type = ?', 'Epaules') + Exercise.where('exercise_type = ?', 'Tris')][0].each do |exercise| %>
    <div class="col-xs-12 col-sm-6 col-md-3">
      <%= render 'exercise_card', exercise: exercise %>
    </div>
  <% end %>
  </div>
</div>

<%= link_to "Créer un nouvel exercice", new_exercise_path, {class: "btn btn-primary"} %>
